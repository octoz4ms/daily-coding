feign:
  sentinel:
    enabled: true

server:
  port: 8082

spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 115.190.171.200:8848
        namespace: public
      config:
        enabled: false
    openfeign:
      sentinel:
        enabled: true
      okhttp:
        enabled: true
      circuitbreaker:
        enabled: true
      client:
        config:
          default:
            connectTimeout: 1000
            readTimeout: 3000
            loggerLevel: basic
          user-service:
            connectTimeout: 1000
            readTimeout: 2000
      compression:
        request:
          enabled: true
          mime-types:
            - text/xml
            - application/xml
            - application/json
          min-request-size: 2048
        response:
          enabled: true
    sentinel:
      transport:
        dashboard: 127.0.0.1:8729
        port: 8722
      eager: true
      web-context-unify: false

logging:
  level:
    com.example.order: INFO
    com.example.feign.client.UserFeignClient: DEBUG
