server:
  port: 8081

spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 115.190.171.200:8848
        namespace: public
      config:
        enabled: false
    sentinel:
      transport:
        dashboard: 127.0.0.1:8729
        port: 8721
      eager: true
      web-context-unify: false

logging:
  level:
    com.example.user: DEBUG
